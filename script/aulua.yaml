project:
  variables:
    AUTHOR: azurite

build:
  out_dir: ../out_script

install:
  out_dir:

scripts:
  - name: ColorHalftone.anm2
    sources:
      - path: ColorHalftone.in.anm2
        variables:
          VERSION: v1.1.0
          INFO: ColorHalftone
