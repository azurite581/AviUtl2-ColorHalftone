project:
  variables:
    AUTHOR: azurite

build:
  out_dir: ../out_script

install:
  out_dir:

scripts:
  - name: カラーハーフトーン.anm2
    sources:
      - path: カラーハーフトーン.in.anm2
        variables:
          VERSION: v1.1.0
          INFO: カラーハーフトーン
